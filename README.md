# ars-nova-ops

**Ars Nova Singers - custom WordPress plugin.** Canonical source (private use; not distributed).

- Current version: 1.0.0 (matches DEV arsnovasingers.kinsta.cloud; header reports 1.0.0)
- Environment: DEV only; not on LIVE.
- Admin-only REST endpoints to install/update/activate/deactivate/delete WordPress plugins by command (wraps core Plugin_Upgrader). DEV automation helper. v1.0.0.

Releases: each tag v<version> attaches the installable zip (top folder ars-nova-ops/). Install/update via the Ars Nova Ops installer or WP-admin upload.
